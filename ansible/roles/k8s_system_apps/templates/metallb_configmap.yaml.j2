apiVersion: v1
kind: ConfigMap
metadata:
  name: config
  namespace: {{ metallb_namespace }}
data:
  config: |
    address-pools:
      - name: default
        protocol: layer2
        addresses:
          - {{ metallb_addresses }}
